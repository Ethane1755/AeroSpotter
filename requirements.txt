# Core CLI + VLM dependencies
exifread>=3.0.0
piexif>=1.1.3
questionary>=2.1.0
rich>=13.7.0
timezonefinder>=6.5.0
requests>=2.32.0
typer>=0.12.0
pillow>=10.0.0
google-genai>=1.0.0
mcp>=1.0.0

# Optional: traffic/offline aircraft enrichment in cli.py
# Use on Python 3.12/3.11 for best compatibility.
# Skip installing these on Python 3.13+ to avoid build/runtime failures.
pandas>=2.2.2,<3; python_version < "3.13"
traffic>=2.13,<3; python_version < "3.13"

# Optional: server/api.py
Flask>=3.0.0
ujson>=5.10.0

# Optional: Airplane_Agent.py
pyautogen>=0.2.0

# Optional: local OCR pipeline (ocr_pipeline.py)
ultralytics>=8.3.0
opencv-python>=4.9.0
paddleocr>=2.8.1
